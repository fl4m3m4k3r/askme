<div class="content-wrapper buffer-both text-center main">
  <div class="buffer-both">
    <h1 class="title-medium">Наши пользователи</h1>
  </div>

  <% @users.each do |user| %>
    <div class="users-list-item">
      <div class="user-image-div user-image-div-small">
        <%= link_to image_tag(user_avatar(user), class: 'user-image'), user_path(user) %>
      </div>
    </div>
  <% end %>

  <% if @hashtags.any? %>
  <div class="hashtags">
    <% @hashtags.each do |hashtag| %>
      <%= link_to "##{hashtag.text}", hashtag_path(hashtag.text), class: 'hashtag'%>
    <% end %>
  </div>
  <% end %>

</div>
